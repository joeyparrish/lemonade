/// <reference types="vite/client" />

// Declares the ambient module types for asset imports such as `import
// "./theme.css"`. TypeScript 7 rejects a side-effect import it has no
// declaration for, where earlier versions let it pass silently.
